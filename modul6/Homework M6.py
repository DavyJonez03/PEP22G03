# Create a function that:
#
# takes in 2 primes numbers (x, y) where x < y
# generates a random number z in the interval 2 to y
# returns (x^z)%y x = pow(self.base, self.__local_secret) % self.prime
# self. = pow(secret, self.local_secret) % self.prime + 129

